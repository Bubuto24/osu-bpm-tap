[project]
name = "osu-bpm-tap"
version = "0.1.0"
description = "Osu tapping bpm training"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "keyboard>=0.13.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
    "ty>=0.0.39",
]
